# overseer (Render account 2)

Heavy engine skeleton:
- registers to gateway
- sends heartbeats
- placeholder job runner
- local "AI assistant" stub (no paid APIs)

## Env
Copy `.env.example` to `.env`.

## Run
```bash
npm i
npm run dev
```
